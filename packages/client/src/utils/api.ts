import axios from "axios";
import { config } from "config";

// export const API = {
//   get: (url: string) =>
//     axios({ method: "GET", url: `${config.apiUrl}/${url}` }),
//   post: (url: string, data) =>
//     axios({ method: "POST", url: `${config.apiUrl}/${url}` }),
//   put: (url: string) =>
//     axios({ method: "PUT", url: `${config.apiUrl}/${url}` }),
//   delete: (url: string) =>
//     axios({ method: "DELETE", url: `${config.apiUrl}/${url}` }),
// };
